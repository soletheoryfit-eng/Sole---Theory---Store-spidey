import { ron, waOrderLink } from "../components/ui";

export default function Home() {
  return (
    <div style={{maxWidth:760,margin:"40px auto",padding:"0 16px"}}>
      <h1 style={{fontSize:32,margin:0}}>SoleTheory — it works ✅</h1>
      <p style={{marginTop:8,color:"#555"}}>
        If you see this, deployment is good. Next we add products, WhatsApp order, admin, etc.
      </p>
    </div>
  );
}
